# Core runtime dependencies
numpy>=1.24
opencv-python-headless>=4.8
scikit-image>=0.21
typer[all]>=0.12
rich>=13
pyyaml>=6
requests>=2.31
Pillow>=10
Jinja2>=3.1

# Optional heavy deps — uncomment as needed:
# torch>=2.1              # Required for DINOv2 embeddings and HF backend
# torchvision>=0.16       # Required for DINOv2 feature extractor
# transformers>=4.40      # Required for HuggingFace Qwen2.5-VL backend
# accelerate>=0.28        # Required for HF transformers multi-GPU
# lpips>=0.1.4            # Required for LPIPS perceptual similarity (Layer 3)
# nvidia-flip             # Required for NVIDIA FLIP metric (Layer 3)
